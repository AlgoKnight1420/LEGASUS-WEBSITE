{
  "name": "legausus-main",
  "private": true,
  "scripts": {
    "build": "cd legasus && npm run build",
    "dev": "cd legasus && npm run dev",
    "install:app": "cd legasus && npm install"
  }
}
